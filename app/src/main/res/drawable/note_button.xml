<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
        <solid android:color="@color/roze" />  <!-- Цвет фона (например, белый) -->
        <corners android:radius="1000dp" />  <!-- Делаем круг (радиус больше размера кнопки) -->
        <stroke
            android:width="2dp"
            android:color="#000000" />  <!-- Чёрная обводка -->
    </shape>